enum sharedPrefrencesKeys {
  autoLogin,
  theme,
  noBoarding,
  loginFirst,
}

enum CacheKeys {
  id,
  name,
  email,
  loggedIn,
}

enum FbCollection {
  users,
  categories,
  products,
  rates,
  carts,
  sliders,
  notifications,
  favorites,
}
